# Auto-activate this project's virtualenv via direnv.
# Create/modify once, then run `direnv allow` in this directory to trust it.
# (You can also just `cd` here and run your existing `venv` alias.)
source .venv/bin/activate
